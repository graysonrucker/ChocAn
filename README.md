# ChocAn
Compilation and execution:
  -Do all compilation and execution from the root directory of the repository.
  -To compile: javac -d out $(find src -name "*.java")
  -To execute: java -cp out chocan.main
